// #include "graphics/text_renderer.h"
// #include "graphics/texture.h"

// bool text_renderer_load_font(TextRenderer &text_renderer);
// void text_renderer_generate_atlas(TextRenderer &text_renderer, stbtt_fontinfo *font, f32 scale);

// TextRenderer text_renderer_init(const char *font_path, f32 font_size) {
//     TextRenderer self{0};
//     usize font_size_bytes;
//     return self;
// }

// void text_renderer_deinit(TextRenderer &renderer) {
//     texture_destroy(renderer.atlas_texture);
//     glDeleteVertexArrays(1, &renderer.vertex_array);
//     glDeleteBuffers(1, &renderer.vertex_buffer);
//     program_destory(renderer.program);
//     free(renderer.font_buffer);
// }
